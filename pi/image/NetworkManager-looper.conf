[device]
# stable MAC while scanning keeps the hotspot solid on the onboard radio
wifi.scan-rand-mac-address=no

[connection]
# don't let the hotspot's shared subnet steal the default route
ipv4.route-metric=100
# never let a USB Wi-Fi adapter doze: power saving drops idle SSH/WebSocket sessions
wifi.powersave=2
